/* --- BEGIN CODE --- */
fun main() {
    let msg = when 2 {
        1, 2, 3, 4 -> {
            "Value is 1, 2, 3, or 4"
        }
        else -> {
            "Value is something else"
        }
    }
    println(msg)
}
/* --- END CODE --- */
/* --- BEGIN OUTPUT ---
Value is 1, 2, 3, or 4
--- END OUTPUT --- */
